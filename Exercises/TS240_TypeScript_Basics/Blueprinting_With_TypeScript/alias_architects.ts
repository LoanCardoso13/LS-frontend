/*

Create a type alias called Contact that represents a contact card. It should include firstName and lastName as strings, age as a number, and isOnline as a boolean.

Expected outcome:

* A type alias Contact that can be used to create contact objects with certain properties.

*/
